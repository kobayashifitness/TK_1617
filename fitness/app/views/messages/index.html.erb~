<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title">hogeさんからのメッセージ</h3>
  </div>
  <div class="panel-body">
	  <% (@messages).each do |message|%>
		  <div>
			  <%= message.message%>
		  </div>
	  <% end %>
	  <%= form_for @message do |message|%>
		  <%= message.text_field :message%>
		  <%= message.hidden :sent_userid, :value => current_user.id%>
		  <%= message.hidden :user_id, :value => @sent_userid %>
		  <%= message.submit "送信"%>
	  <% end %>
  </div>	
